{{> header navbar_class="navbar"}}
<div class="container">
  <div class="row">
      <div class="col s12">
        <h1 class="header center">{{city.label}}</h1>

        <p class="center">
          Pour connaître le lieu des rassemblements,
        </p>
        <p class="center">
          Pour ajouter des informations sur votre Nuit Debout,
        </p>
        <p class="center">
          Pour collecter le travail des commissions et le mettre en forme,
        </p>
        <p class="center">
          <a href="{{city.wiki_url}}" class="waves-effect waves-light btn-large">Venez sur le Wiki.</a>
        </p>

        <div class="collection center">
          {{#each city.links}}
            <a href="{{this}}" class="collection-item">{{this}}</a>
          {{/each}}
        </div>

        <p class="center">Vous voulez compléter cette page, ajouter des liens ? Venez sur le <a href="http://chat.nuitdebout.fr">chat</a>.</p>
      </div>
  </div>
</div>
{{> footer }}
